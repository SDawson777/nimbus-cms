VITE_NIMBUS_API_URL=https://<YOUR-API-URL>/api/v1/nimbus
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0

# Sanity CMS configuration
VITE_SANITY_PROJECT_ID=ygbu28p2

# Sentry error reporting + performance monitoring
# Get your DSN from https://sentry.io/organizations/*/issues/
VITE_SENTRY_DSN=https://YOUR_SENTRY_DSN@o4509708991397888.ingest.us.sentry.io/4510547370770432

# Theme configuration (name of the theme to load at runtime)
VITE_NIMBUS_THEME=default

# Public Mapbox token exposed to the browser (if you use Mapbox GL).
VITE_NIMBUS_HEATMAP_MAPBOX_TOKEN=pk.replace_me

# Preview secrets must NOT be placed in client-side envs (VITE_*). Configure
# `PREVIEW_SECRET` on the API server only. The frontend will open Studio links
# without embedding preview secrets.
