SANITY_STUDIO_PROJECT_ID=ygbu28p2
SANITY_STUDIO_DATASET=production

# Optional: if you are reverse-proxying the Studio under a subpath (e.g. /studio)
# set this to that path. For Sanity-hosted studios (e.g. *.sanity.studio), leave as '/'.
SANITY_STUDIO_BASE_PATH=/

# NOTE: Do NOT put secrets in client-visible envs (VITE_*). Keep tokens
# such as preview secrets and private Mapbox tokens on the server-side.
# If you need a public Mapbox token for client maps, use `VITE_MAPBOX_TOKEN`.
VITE_MAPBOX_TOKEN=pk.replace_me
