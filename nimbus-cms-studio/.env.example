SANITY_PROJECT_ID=
SANITY_DATASET=staging

# Note: Keep secrets (preview tokens, server Mapbox tokens, JWT secrets)
# in server-side environment variables (do not add VITE_ prefixed secrets).
VITE_MAPBOX_TOKEN=your_public_mapbox_token
